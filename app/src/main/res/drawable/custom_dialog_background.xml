<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="20dp"/> <!-- Độ bo tròn góc, có thể điều chỉnh -->
    <solid android:color="@color/color_white"/> <!-- Màu nền của dialog -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray"/> <!-- Viền (tùy chọn) -->
</shape>